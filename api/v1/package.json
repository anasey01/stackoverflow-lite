{
  "name": "stackoverflow-lite",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "babel": "rm -rf server-babel.js && babel server/server.js -o server/server-babel.js",
    "start": "node bin/dev",
    "clean": "rm dist -rf",
    "build": "npm run clean && mkdir dist && babel server -s -d dist",
    "production": "npm run build && node bin/production",
    "lint": "./node_modules/.bin/eslint ./app/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Anani Oluseyi",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.3",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-stage-2": "^6.24.1",
    "babel-register": "^6.26.0",
    "eslint": "^5.3.0",
    "mocha": "^5.2.0"
  },
  "dependencies": {
    "express": "^4.16.3"
  }
}
