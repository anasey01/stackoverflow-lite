// eslint-disable-next-line no-console
const log = item => console.log(item);

const formatPostQuestion = (data) => {
  // let newData = [];
  // data.map((x) => {
  //   newData.push({

  //   });
  // });
};

export default {
  log,
  formatPostQuestion,
};
